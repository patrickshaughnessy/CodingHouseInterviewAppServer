{
  "name": "InterviewAppServer",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "start": "echo Starting app without explicit configuration && echo Check npm run for options && node app.js",
    "test": "source .env/test && mocha --recursive",
    "start:dev": "source .env/development && DEBUG=InterviewAppServer:* nodemon app.js",
    "start:prod": "source .env/production && DEBUG=InterviewAppServer:* node app.js"
  },
  "dependencies": {
    "body-parser": "~1.13.2",
    "compression": "^1.6.2",
    "cookie-parser": "~1.3.5",
    "cors": "^2.7.1",
    "debug": "^2.2.0",
    "express": "~4.13.1",
    "jade": "~1.11.0",
    "lodash.merge": "^4.5.1",
    "mongoose-deep-populate": "^3.0.0",
    "morgan": "~1.6.1",
    "rootpath": "^0.1.2",
    "serve-favicon": "~2.3.0"
  },
  "devDependencies": {
    "chai": "^3.5.0",
    "mocha": "^3.0.2",
    "supertest": "^2.0.0"
  }
}
